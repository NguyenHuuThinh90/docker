pipeline {
    agent any
    environment {
        MAVEN_PATH = 'k8e-jk-git/pom.xml'
    }
    stages {
        stage("Clean") {
            steps {
                echo "${MAVEN_PATH}"
                bat "mvn -f  ${MAVEN_PATH} clean"
            }
        }
        stage('Build') {
            steps {
                bat "mvn -f  ${MAVEN_PATH} install"
            }
            post {
                success {
                    echo "DONE"
                }
            }
        }
    }
}